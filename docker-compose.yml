version: '2'
services:

  bubuku:
    build: .
    depends_on:
      - zookeeper
    environment:
      BUBUKU_MODE: "local"
      HEALTH_PORT: "8080"
      BUKU_FEATURES: ""

  zookeeper:
    image: wurstmeister/zookeeper:3.4.6
